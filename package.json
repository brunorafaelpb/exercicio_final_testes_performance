{
  "name": "api_exercicio_final",
  "version": "1.0.0",
  "description": "Esta API permite o registro e login de usuários, além do gerenciamento de produtos em estoque. Todas as operações de produto exigem autenticação via JWT.",
  "main": "app.js",
  "scripts": {
    "test": "mocha 'test/**/*.test.js' --timeout 10000 --reporter mochawesome",
    "test-controller-rest": "mocha 'test/rest/controller/*.test.js' --timeout 10000 --reporter mochawesome",
    "test-external-rest": "mocha 'test/rest/external/*.test.js' --timeout 10000 --reporter mochawesome",
    "test-external-graphql": "mocha 'test/graphql/external/*.test.js' --timeout 10000 --reporter mochawesome",
    "start": "node server.js",
    "start-graphql": "node graphql/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.12.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^13.0.0",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "chai": "4.5.0",
    "chai-exclude": "^2.1.1",
    "mocha": "^11.7.1",
    "mochawesome": "^7.1.3",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  }
}
